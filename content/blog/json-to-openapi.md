---
title: "JSON 转 OpenAPI 规范教程"
description: "根据 API 返回 JSON 生成 OpenAPI/Swagger 规范片段，用于接口文档化。"
date: "2026-08-19"
category: "代码生成"
keywords: ["json转openapi","openapi","swagger"]
---

# JSON 转 OpenAPI 规范教程

根据 API 返回的 JSON 样本生成 OpenAPI/Swagger 规范的 schema 片段，用于接口文档化。

## 生成内容

components/schemas 定义、属性类型、示例值、必填字段。

## 使用场景

快速为现有 API 补充 OpenAPI 文档，导入 Swagger UI。

## 代码示例

```json
{"id": 1, "name": "x"}
```

## 推荐在线工具

- [/codegen/java](/codegen/java)
- [/format](/format)

## 小结

根据 API 返回 JSON 生成 OpenAPI/Swagger 规范片段，用于接口文档化。 JSONDog 提供全套 JSON 在线工具，纯前端、零上传、中英双语，欢迎免费使用。
